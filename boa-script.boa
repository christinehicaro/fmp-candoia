# get projects stats
p: Project = input;

methods : output of sum[string] of int;

visit(p, visitor {
    before methodType : Method -> {
        if (methodType != "")
          	methods[methodType.name] << 1;
    }
});
